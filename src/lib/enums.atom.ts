export enum DirType {
  RIGHT = 'right',
  LEFT = 'left',
}

export enum ButtonType {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  // TEXT = 'text',
}

export enum TextType {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ERROR = 'error',
  WHITE = 'white',
}

export enum CheckBoxtype {
  CHECKBOX = 'checkbox',
  RADIOCHECK = 'radio',
  SWITCHCHECK = 'switccheck',
}
