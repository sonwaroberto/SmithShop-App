import productSlice from '../features/product/slices/product.slice';
// import translationSlice from '../features/translations/translation.slice';
import authSlice from '../features/auth/auth.slice';

export default {
  // translationSlice,
  productSlice,
  authSlice,
};
